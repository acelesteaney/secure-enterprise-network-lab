# HTTP Server

An internal HTTP server was deployed in the server VLAN.

The server is accessible from the authorized client VLANs.

HTTP connectivity was tested to validate the network services and ACL policy.
