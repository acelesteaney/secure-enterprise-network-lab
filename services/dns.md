# DNS

A DNS server was deployed in the server VLAN.

DNS allows clients to resolve internal domain names without relying on external DNS infrastructure.

DNS connectivity was tested from client networks.
